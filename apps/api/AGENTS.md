# Ously API Context

- **Framework**: Hono on Cloudflare Workers.
- **Conventions**:
  - Use `@ously/validation` for request/response validation.
  - Use `@ously/auth` for authorization.
  - Access database using schema from `@ously/db`.
